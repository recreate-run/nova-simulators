version: "2"
sql:
  - engine: "sqlite"
    queries: "pkg/database/queries"
    schema: "migrations"
    gen:
      go:
        package: "database"
        out: "pkg/database"
        emit_json_tags: true
        emit_interface: false
        emit_empty_slices: true
