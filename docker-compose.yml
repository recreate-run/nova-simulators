version: '3.8'

services:
  slack:
    build:
      context: ./simulators/slack
      dockerfile: Dockerfile
    ports:
      - "9001:9001"
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
    container_name: slack-simulator
